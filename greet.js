// Create a function called greet that takes a name parameter and logs a greeting to that name in the function.
// Create a file named names.txt.
// List 5 names (as strings) csv format in that file.
let names = ["Isaac", "Leo-Juan", "Santiago", "Jensen", "Daniel"]

function greet(name) {
    console.log(`Hello, ${name}.`);
};

module.exports = greet;